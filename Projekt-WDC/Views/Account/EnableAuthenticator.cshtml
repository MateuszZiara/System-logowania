@model Projekt_WDC.ViewModels.EnableAuthenticatorViewModel
@{
    ViewData["Title"] = "Skonfiguruj aplikację uwierzytelniającą";
}

<div class="auth-wrapper" style="max-width: 600px;">
    <div class="card">
        <div class="card-header">
            <h2 class="card-title">Konfiguracja 2FA</h2>
            <p class="text-muted small">Zwiększ bezpieczeństwo swojego konta</p>
        </div>
        <div class="card-body">
            <p>Aby użyć aplikacji uwierzytelniającej, wykonaj następujące kroki:</p>
            <ol class="list-group list-group-numbered mb-4">
                <li class="list-group-item border-0">
                    <p>Pobierz aplikację do uwierzytelniania dwuskładnikowego, taką jak Microsoft Authenticator lub Google Authenticator.</p>
                </li>
                <li class="list-group-item border-0">
                    <p>Zeskanuj kod QR lub wprowadź klucz <kbd>@Model.SharedKey</kbd> w swojej aplikacji uwierzytelniającej.</p>
                    <div id="qrCode" class="text-center my-3"></div>
                    <div id="qrCodeData" data-url="@Model.AuthenticatorUri"></div>
                </li>
                <li class="list-group-item border-0">
                    <p>Po zeskanowaniu kodu QR lub wprowadzeniu klucza, aplikacja wygeneruje unikalny kod. Wpisz go w polu poniżej.</p>
                    <form method="post">
                        <div class="form-floating mb-3">
                            <input asp-for="Code" class="form-control" autocomplete="off" placeholder="Kod weryfikacyjny" />
                            <label asp-for="Code">Kod weryfikacyjny</label>
                            <span asp-validation-for="Code" class="text-danger small"></span>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Weryfikuj</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger mt-2 small"></div>
                    </form>
                </li>
            </ol>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <script type="text/javascript">
        window.addEventListener("load", function () {
            new QRCode(document.getElementById("qrCode"),
                {
                    text: "@Html.Raw(Model.AuthenticatorUri)",
                    width: 150,
                    height: 150
                });
        });
    </script>
}
